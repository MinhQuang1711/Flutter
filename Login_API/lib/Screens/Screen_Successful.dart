import 'package:flutter/material.dart';

class Login_Successful extends StatelessWidget {
  const Login_Successful({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('Hello'),
        ),
      ),
    );
  }
}
